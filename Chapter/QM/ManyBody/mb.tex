\documentclass[../../../main.tex]{subfiles}
\begin{document}
In framework of many-body system, the fundamental object is the many-body wavefunction
\[
  \Psi(x_1,\ldots,x_N)
  =\Psi(\mathbf{r}_1,\sigma_1\dots,\mathbf{r}_N,\sigma_N)
\]
where the generalized coordinate $x = (\mathbf{r}, \sigma)$ takes into account the spatial coordinate $\mathbf{r}$ but also the intrinsic spin $\sigma$ pertaining to the particle.
The state of the system therefore lives in the tensor product Hilbert space $\mathcal{H}^{\otimes N}$.
For identical particles, the wavefunction must satisfy permutation symmetry,
\begin{equation*}
  \Psi(\ldots, x_i, \ldots, x_j, \ldots) = \pm \Psi(\ldots, x_j, \ldots, x_i, \ldots)
\end{equation*}

When the particle number becomes macroscopic $  N \rightarrow O(10^{23})$ and the system is described only through macroscopic observable rather than the full wavefunction, one replaces the pure state description $\Psi(x_1,\ldots,x_N)$ with an ensemble or density operator
\begin{equation*}
  \rho = \frac{e^{-\beta H}}{Z}, \quad Z = \text{Tr} \, e^{-\beta H}.
\end{equation*}
This quantum statistical mechanics step corresponds to assuming statistical equilibrium with a reservoir (canonical ensemble) or fixed energy (microcanonical ensemble).

Thermodynamics emerges when an additional limit is taken
\begin{equation*}
  N \to \infty, \quad V \to \infty, \quad \frac{N}{V} = \text{const.}
\end{equation*}
In this regime microscopic fluctuations become negligible compared with macroscopic averages.
Observables become smooth functions of intensive variables such as \(T, P,\) and \(\rho\).
Thermodynamic relations follow from derivatives of the free energy
\begin{equation*}
  F = -k_B T \ln Z, \quad U = -\frac{\partial}{\partial \beta} \ln Z
\end{equation*}
For example
\begin{equation*}
  S = -\left( \frac{\partial F}{\partial T} \right)_V, \quad P = -\left( \frac{\partial F}{\partial V} \right)_T
\end{equation*}
Thermodynamic quantities are obtained from the partition function,

Solid-state theory arises when the many-body system satisfies essential conditions
\begin{enumerate}
  \item The particles form a dense phase (typically electrons and ions).
  \item The ions form an approximately periodic lattice
        \begin{equation*}
          V(\mathbf{r} + \mathbf{R}) = V(\mathbf{r})
        \end{equation*}
        where \(\mathbf{R}\) is a Bravais lattice vector. In this condition, eigenstates satisfy Bloch's theorem $\psi_{nk}(\mathbf{r}) = e^{i \mathbf{k} \cdot \mathbf{r}} u_{nk}(\mathbf{r})$
  \item The electronic states can be treated in an effective single-particle framework due to screening and mean-field approximations.
\end{enumerate}

The many-body problem is then approximated by quasiparticles (electrons, phonons, magnons) moving in effective potentials.
Techniques such as Hartree–Fock, density functional theory, or Fermi liquid theory implement this reduction.

\subsection{Identical Particle}
Before imposing indistinguishability, an $N$-particle state is written as
\begin{equation*}
  |k_1, k_2, \ldots, k_N\rangle = |k_1\rangle \otimes \ldots \otimes |k_N\rangle
\end{equation*}
This state is not symmetric in general.
If we exchange two slots,
\begin{equation*}
  \mathcal{T}_{12}|k_1, k_2, \ldots \rangle = |k_2, k_1, \ldots \rangle
\end{equation*}
we obtain is a different vector.

Indistinguishability implies that the Hamiltonian cannot change if the degrees of freedom of two arbitrary particles $i$ and $j$ are exchanged
\begin{equation*}
  {H } = {H} (\dots,i\dots,j,\dots) = {H} (\dots,j\dots,i,\dots)
\end{equation*}
This can be seen by acting with the operator exchanging two arbitrary particles, $i$ and $j$, on a state of $N$ identical particles
\begin{equation*}
  \mathcal{T }_{ij} \ket{\dots, \phi_i,\dots,\phi_j,\dots} = \ket{\dots, \phi_j,\dots,\phi_i,\dots}
\end{equation*}
which produces a state that may not be physically different from the original state, so that there can only be a phase factor $\nu_{ij }=e ^{i\alpha_{ij}}$
\begin{equation*}
  \mathcal{T }_{ij} \ket{\dots, \phi_i,\dots,\phi_j,\dots} = \nu_{ij }\ket{\dots, \phi_i,\dots,\phi_j,\dots}
\end{equation*}
Moreover, because of $\mathcal{T}_{ij }=\mathcal{T}_{ji}$ we have
\begin{equation*}
  \mathcal{T}_{ij }^{2 }=1\qquad\nu_{ij }=\pm1
\end{equation*}
These results imply that there are only two types of quantum particles
\begin{equation*}
  \nu = \begin{cases}
    +1, & \text{ particles whose symmetrical under particle exchange}     \\
    -1, & \text{ particles whose antisymmetrical under particle exchange}
  \end{cases}
\end{equation*}

\subsubsection{Boson.}
Consider the state
\begin{equation*}
  |k_1, \ldots, k_N\rangle = |k_1\rangle \otimes |k_2\rangle \otimes \ldots \otimes |k_N\rangle
\end{equation*}
The subscript in $\ket{k_n}$ labels the one-particle state in that tensor slot.
In order to produce physical multiparticle boson state, where the particles are truly indistinguishable, we introduce symmetrization $S$ for a fixed number $N$ of particles
\begin{equation*}
  S |k_1, \ldots, k_N \rangle =
  \frac{1}{\sqrt{N!N_1! \ldots N_M!}}
  \sum_{p \in S_N} |k_{p(1)}, \ldots, k_{p(N)} \rangle
\end{equation*}
which satisfy $\nu=1$.
The symmetrization operator  $S$ acts on the tensor-product slots, not on the physical particles themselves, where it rearranges the positions of the one-particle states in the tensor product
\begin{equation*}
  |k_1, k_2, \ldots, k_N\rangle \rightarrow |k_{p(1)}, k_{p(2)}, \ldots, k_{p(N)}\rangle
\end{equation*}

The is run over $p$ all permutations $p$ of the set $\mathcal{N}=\left\{ 1,\dots,N \right\} $, which there are $N!$ element, hence the $1/\sqrt{N!}$ normalization constant.

Now suppose that $N_l$ particle occupy $k_l$ state.
Many permutations now produce identical terms, since swapping two particles that are both in $k_l$  does not change the vector, hence $1/\sqrt{N_l!}$ normalization constant.

Each term is the original tensor-product state with tensor slots rearranged according to $p$, such that $\ket{\dots k_{p(n)}\dots}$ is placed in slot $n$.
For example, with $p(n)=n+1$, the state $S\ket{k_1}$ gets mapped into
\begin{equation*}
  \ket{k_{{(1)}}\dots} \mapsto \ket{\dots \ket{k_2}\dots}
\end{equation*}

\subsubsection{Fermion.}
With the same state $|k_1, \ldots, k_N\rangle $, the antisymmetrization is defined as
\begin{equation*}
  A|k_1,k_2,\ldots,k_N\rangle = \frac{1}{\sqrt{N!}} \sum_{p \in S_N} \mathrm{sgn}(P) |k_{i_1},k_{i_2},\ldots,k_{i_N}\rangle
\end{equation*}
As the case with the symmetrization operator, the sum is run over $p$ all permutations $p$ of the set $\left\{ 1,\dots,N \right\} $.
The only difference is in the parity factor
\begin{equation*}
  \mathrm{sgn}(p) =
  \begin{cases}
    +1 & \text{ Even permutations} \\
    -1 & \text{ Odd permutations}
  \end{cases}
\end{equation*}
This factor express that the state antisymmetry under particle exchange.
Since any permutation $p$ of $N$ slots can be written as a product of transpositions
\begin{equation*}
  p = \mathcal{T}_{i_1j_1} \mathcal{T}_{i_2j_2} \ldots\mathcal{T}_{i_mj_m}.
\end{equation*}
the parity of the permutation is determined by the number of transpositions $m$ in this decomposition: even $m$ for $+1$ and odd $m$ for $-1$.

This parity is the origin of Pauli exclusion principle.
The probability for a many-particle state to contain two or more Fermions in the same single-particle state vanishes.
In another important contribution, Wolfgang Pauli (1940) proved by his famous spin-statistics theorem that Bosons carry integer spin, $s=1,\dots$ , while Fermions carry half-integer spin, $s = 1/2, \dots$.

|\subsubsection{The application.}
The symmetric and antisymmetrical operator is used to construct boson and fermion subspace from the $n $-particle Hilbert space
\begin{equation*}
  \mathcal{H}^{\otimes n}  =
  \underbrace{\mathcal{H} \otimes \dots \otimes \mathcal{H}}_n =
  \bigotimes_n \mathcal{H}
\end{equation*}
The symmetric subspace is defined as the set of all vectors invariant under permutation of slots
\begin{equation*}
  \mathrm{Sym}^n(\mathcal{H}) = \left\{ |\Psi\rangle \in \mathcal{H}^{\otimes n} \mid P|\Psi\rangle = |\Psi\rangle \text{ for all permutations } P \right\}
\end{equation*}
with $P_p$ as the linear operator representing $\mathcal{H}^{\otimes n}$.
Equivalently,
\begin{equation*}
  \mathrm{Sym}^n(\mathcal{H}) = S\mathcal{H}^{\otimes n}
\end{equation*}
where the symmetrization projector action is
\begin{equation*}
  S = \frac{1}{n!} \sum_{p \in S_n} P_p
\end{equation*}
Note that the normalization constant is $1/n!$ instead of $1/\sqrt{n!}$ to satisfy idempotency $S^2=S$ for subspace projection.
Meanwhile, the antisymmetrical subspace is defined as the set of vectors that change sign under odd permutations
\begin{equation*}
  \wedge^n(H) = \left\{ |\Psi\rangle \in \mathcal{H}^{\otimes n} \,|\, P |\Psi\rangle = \mathrm{sgn}(P) |\Psi\rangle \right\}
\end{equation*}
Equivalently,
\begin{equation*}
  \wedge^n(H) = A \mathcal{H}^{\otimes n}
\end{equation*}
with the action of antisymmetrization as
\begin{equation*}
  A = \frac{1}{n!} \sum_{p \in S_n} \text{sgn}(p) P
\end{equation*}

With these in mind, the Fock subsspace for both boson and fermion can be derived from the full Fock space as
\begin{equation*}
  \mathcal{F}_B = \bigoplus_{n=0}^{\infty} \mathrm{Sym}^n(\mathcal{H}) \qquad
  \mathcal{F}_F = \bigoplus_{n=0}^{\infty} \wedge^n(\mathcal{H})
\end{equation*}

\subsection{Identical Particle Wavefunction}
The arbitrary product state $  |k_1, \ldots, k_N\rangle$, which do not in general obey symmetrization nor antisymmetrization condition, can be projected into boson or fermion subspace using symmetrization and antisymmetrization operator
\begin{gather*}
  S |k_1, \ldots, k_N \rangle =
  \frac{1}{\sqrt{N!N_1! N_1! \ldots N_M!}}
  \sum_{p \in S_N} |k_{p(1)}, \ldots, k_{p(N)} \rangle  \\
  A|k_1,k_2,\ldots,k_N\rangle = \frac{1}{\sqrt{N!}} \sum_{p \in S_N} \mathrm{sgn}(P) |k_{i_1},k_{i_2},\ldots,k_{i_N}\rangle
\end{gather*}
To determine the wavefunction of boson and fermion, one construct $N \times N$ matrix
\begin{equation*}
  M =
  \begin{bmatrix}
    \phi_1(x_1) & \ldots & \phi_1(x_N) \\
    \vdots      & \ddots & \vdots      \\
    \phi_N(x_1) & \ldots & \phi_N(x_N)
  \end{bmatrix}
\end{equation*}
where $\phi_i $ denote quantum state and $\phi(x_j)$ labels the particle coordinate.

\subsubsection{Boson.}
The wavefunction  in boson subspace is represented as
\begin{equation*}
  \Psi_B(x_1, \ldots, x_N) = \frac{1}{\sqrt{N!N_1! \ldots N_M!}} \;\mathrm{perm}(M)
\end{equation*}
Written explicitly,
\begin{equation*}
  \Psi_B(x_1, \ldots, x_N)
  = \frac{1}{\sqrt{N!\prod_i N_i!}} \sum_{P \in S_N} \prod_{i=1}^N \phi_i(x_{P(i)})
\end{equation*}

In the Bose–Einstein condensate, all $N$ particles occupy the same single–particle ground orbital $\phi_0(x)$, hence permutation produces the same term
\begin{equation*}
  \prod_{j=1}^{N} \phi_0(x_{P(j)}) = \phi_0(x_1)\phi_0(x_2)\ldots\phi_0(x_N)
\end{equation*}
Since there are $N!$ permutations
\begin{equation*}
  \sum_{P\in S_N} \prod_{j=1}^{N} \phi_0(x_{P(j)}) = N! \phi_0(x_1)\phi_0(x_2)\ldots\phi_0(x_N)
\end{equation*}
The normalization factor becomes
\begin{equation*}
  \frac{1}{\sqrt{N!N!}} = \frac{1}{N!}
\end{equation*}
Hence the many-body wavefunction reduces to
\begin{equation*}
  \Psi(x_1,\ldots,x_N) = \phi_0(x_1)\phi_0(x_2)\ldots\phi_0(x_N)
\end{equation*}

\subsubsection{Fermion.}
Rather with permanent, we use determinant, also called Slater determinant
\begin{equation*}
  \Psi_F(x_1, \ldots, x_N) = \frac{1}{\sqrt{N!}} \det(M)
\end{equation*}
Expanding the determinant gives
\begin{equation*}
  \Psi_F(x_1, \ldots, x_N) = \frac{1}{\sqrt{N!}} \sum_{P \in S_N} \text{sgn}(P) \prod_{i=1}^N \phi_i(x_{P(i)})
\end{equation*}
The determinant will enforce sign change under particle exchange and vanishing upon particle occupying the same state.

\subsection{Non-interacting Identical Particles}
The quantum Hamiltonian of \( N \) identical non-interacting particles is given by
\begin{equation*}
  \hat{H}_0 = \sum_{i=1}^{N} \hat{h}(x_i)
\end{equation*}
where \( \hat{h}(x) \) is the single-particle Hamiltonian.
Usually the single-particle Hamiltonian is given by
\begin{equation*}
  \hat{h}(x) = -\frac{\hbar^2}{2m} \nabla^2 + U(r)
\end{equation*}
with \( U(r) \) the external confining potential.
In general the single-particle Hamiltonian \( \hat{h} \) satisfies the eigenvalue equation
\begin{equation*}
  \hat{h}(x) \phi_n(x) = \epsilon_n \phi_n(x)
\end{equation*}
where $\epsilon_n $ are the single-particle eigenenergies and $\phi_n(x)$ the single-particle eigenfunctions.

\subsubsection{Uniform gas of non-interacting fermions.}
The term uniform refers to  the fact that particle density is spatially constant
\begin{equation*}
  n (\mathbf{r }  ) = \frac{N }{V }= \text{const.}
\end{equation*}
such that the system is translationally invariant and is no external potential nor spatial structure.
The term non-interacting gas imply that only kinetic term contribute to the Hamiltonian
\begin{equation*}
  \hat{H}_0 = \sum_{i=1}^{N} \frac{\hbar ^2 \mathbf{k }_i ^{2 }}{2m}
\end{equation*}
This is equivalent to setting the temperature into zero.
The term fermion obviously refers to the fact that we are considering fermion case.
Since the eigenstates of free particle is a plane wave, the eigenstates of fermion is this case include extra spinor $\chi_\sigma$ factor
\begin{equation*}
  \psi(x) = \frac{1}{\sqrt{V}} e^{i \mathbf{k } \cdot \mathbf{r}} \chi_\sigma
\end{equation*}
Without any boundary condition, the spectrum of $\mathbf{k }$ is continuous.
Further consideration include periodic boundary condition, not periodic potential, which quantize the wavevector.
The results of this include the Fermi energy
\begin{equation*}
  \epsilon_F = \frac{\hbar^2 k_F^2}{2m} = \frac{\hbar^2 }{2m} \left( 3\pi^2 \rho \right)^{2/3}
\end{equation*}
which is defined as the energy of maximum occupation state at zero temperature, and energy density
\begin{equation*}
  \mathcal{E} = \frac{E}{V} = \frac{3}{5} \rho \varepsilon_F = \frac{3}{5} \cdot \frac{h^2}{2m} \left( 3\pi^2 \right)^{2/3} \rho^{5/3}
\end{equation*}

\subsubsection{Derivation.}
We start by imposing periodic boundary of cube with side $L$, such that the wavefunction satisfy
\begin{equation*}
  e ^{i \mathbf{k }(\mathbf{r }+\mathbf{L })}
  +e ^{i \mathbf{k }\mathbf{r }}
\end{equation*}
This imply $  e ^{i \mathbf{k } \mathbf{L }}=1$ or
\begin{equation*}
  \mathbf{k }=\frac{2\pi }{L }\mathbf{n}
\end{equation*}
Summing over the integers or summing over the corresponding discrete $\mathbf{k}$-values is equivalent
\begin{equation*}
  \sum_{\mathbf{k }f(\mathbf{k } )} \to
  \sum_{\mathbf{n}}f(\mathbf{k }(\mathbf{n} ))
\end{equation*}
since allowed $k$-state corresponds to a unique integer triple $\mathbf{n}$.
We are still counting over still counting discrete states exactly, each integer corresponds exactly to one allowed $k$.
In other hand, for continuous case
\begin{equation*}
  \int d^3 \mathbf{n }\to
  \frac{V }{(2\pi)^3}\int d^3 \mathbf{k}
\end{equation*}
the same case does not apply.
Here, the factor $V/(2\pi)^3$ denote a volume discrete state occupies in $k$-space  $d^3\mathbf{k}$, and is related by $  \mathbf{k }=2\pi\mathbf{n}/L$.

In the thermodynamic limit $L \rightarrow \infty$, this meant that the number of Fermion can be approximated as
\begin{equation*}
  \sum_{\mathbf{k }} \to
  \frac{V }{(2\pi)^3} \int d^3 \mathbf{k} f(\mathbf{k } )
\end{equation*}
The distribution for arbitrary temperature is given by the Fermi function
\begin{equation*}
  f(\epsilon_\mathbf{k},T)=\frac{1}{\exp\left[(\epsilon_\mathbf{k}-\mu)/k_BT\right]+1}
\end{equation*}
In the zero temperature, this function simplify into step function
\begin{equation*}
  f(\epsilon_\mathbf{k},T) \equiv\Theta(\epsilon_F-\epsilon_\mathbf{k}) =
  \begin{cases}
    1 & \epsilon_k<\mu=\epsilon_F \\
    0 & \mu<\epsilon_k
  \end{cases}
\end{equation*}
or in terms of wavevector
\begin{equation*}
  \Theta(k_F-k) =
  \begin{cases}
    1 & k<\epsilon_F \\
    0 & \epsilon_F<k
  \end{cases}
\end{equation*}
which is obtained by dividing both argument by $\hbar ^2/2m$.

With this in mind, the total number of fermion can now be evaluated as
\begin{equation*}
  N = \sum_{\sigma} \frac{V }{(2\pi)^3 } \int d^3\mathbf{k} \; \Theta \left( \epsilon_F - \frac{\hbar ^2 \mathbf{k}^2 }{2m} \right)
\end{equation*}
This best be evaluated in spherical coordinate
\begin{align*}
  N & =   \frac{2V }{(2\pi)^3 }     \int_{0}^{2\pi} \int_{0}^{ \pi} \int_{0}^{\infty}dk\;d\Omega \;\Theta \left( k_F-k \right)   k^2 \sin \theta        \\
  N & =  \frac{2 V}{(2\pi)^{3}} \int_{0}^{k_{F}}dk\;  4\pi k^{2} = \frac{2 V}{(2\pi)^{3}} \cdot \frac{4\pi}{3} k_{F}^{3} = \frac{V}{3\pi^{2}} k_{F}^{3} \\
\end{align*}
Alternatively, without using $k_F$ and solving in terms of Fermi energy
\begin{equation*}
  N =  c \int\displaylimits_{0}^{\sqrt{2m\epsilon_F}/\hbar }dk\;  k^{2} = \frac{2 V}{(2\pi)^{3}} \frac{4\pi}{3} \left( \frac{\sqrt{2m\epsilon_F} }{\hbar } \right) ^{3} =  \frac{V}{3\pi^{2}} \left( \frac{2m\epsilon_F }{\hbar^2 } \right)^{3/2}
\end{equation*}
This ultimately yields
\begin{equation*}
  \epsilon_F = \frac{\hbar ^2 k_F^2}{ 2m} = \frac{\hbar ^2 }{2m }\left( 3\pi^2 \frac{N }{V } \right)^{1/3}
\end{equation*}
As a uniform gas, the density is constant
\begin{equation*}
  \rho = \frac{1}{3\pi^{2}} k_{F}^{3} = \frac{1}{3\pi^{2}} \left( \frac{2m\epsilon_F }{\hbar ^2 } \right) ^{3/2}
\end{equation*}

The total energy could be evaluated in the same method
\begin{align*}
  E & =  \sum_{\sigma,\mathbf{k }}  \epsilon_{\mathbf{k}} \Theta \left(k_F-k\right)
  =\sum_{\sigma} \frac{V}{(2\pi)^3} \int d^3 \mathbf{k}\; \frac{\hbar^{2}k^{2}}{2m} \Theta \left( \epsilon_{F} - \frac{\hbar^{2}k^{2}}{2m} \right) \\
    & =  \frac{4\pi V \hbar ^2}{(2\pi)^{3}m} \int_{0}^{k_{F}}dk\;  k^{4}
  = \frac{V \hbar ^2}{10\pi^{2}m} k_{F}^{5}
  = \frac{V \hbar ^2}{10\pi^{2}m} \left( 3\pi^2 \rho\right) \frac{2m\epsilon_F }{\hbar ^2}                                                         \\
  E & = \frac{3 }{5 }V \rho \epsilon_F = \frac{3 }{5 }\frac{\hbar ^2 }{2m }V(3\pi^2  )^{2/3}\rho^{5/3}
\end{align*}
On dividing by $V$ we obtain the energy density.

\subsection{Many-electron Atom}
The quantum Hamiltonian of $N$ identical interacting particles is given by
\begin{equation*}
  \hat{H} = \sum_{i=1}^{N} \hat{h}(x_i) + \frac{1}{2} \sum_{\substack{i,j=1 \\ i \neq j}}^{N} V(x_i, x_j) = \hat{H}_0 + \hat{H}_1
\end{equation*}
where $\hat{h}$ is the single-particle Hamiltonian and $V(x_i, x_j)$ is the inter-particle potential of the mutual interaction.
In the case of non-relativistic quantum mechanics of $N$ electrons bound to a nucleus of charge $+Ze$, the Hamiltonian reads
\begin{equation*}
  \hat{H} = \sum_{i=1}^{N} \left( -\frac{\hbar^2}{2m} \nabla_i^2 - \frac{Ze^2}{4\pi \epsilon_0 r_i} \right) + \sum_{i<j} \frac{e^2}{4\pi\epsilon_0 |r_i - r_j|}
\end{equation*}
The single particle Hamiltonian consist of the kinetic energy of electron $i$ and Coulomb attraction between that electron and the nucleus.
The inter-particle Hamiltonian represents the Coulomb repulsion between pairs of electrons.
It uses summation range $i<j$ so that each pair is counted once, which is equivalent to the factor $1/2$ in the more symmetric form.
\begin{equation*}
  \hat{H}_1=\frac{1}{2} \sum_{\substack{i,j=1 \\ i \neq j}}^{N} V(x_i, x_j)
\end{equation*}
The key difficulty of the many-electron problem arises from the second term, because it couples the coordinates of all electrons and prevents the Schrödinger equation from separating into independent single-particle equations.

\subsubsection{Non-interacting Hamiltonian.}
Suppose assumption that each electron moves independently in the electrostatic field of the nucleus, and that electrons do not repel one another.
This crude assumption of non-interacting electrons is equivalent to ignoring the inter-particle Hamiltonian
\begin{equation*}
  \hat{H} = \hat{H}_0 = \sum_{i=1}^{N} \hat{h}(x_i)
\end{equation*}

Despite that, the model predicts correctly the following results.
\begin{enumerate}
  \item \textbf{Electrons occupy discrete orbitals.} The wavefunction satisfy
        \begin{equation*}
          \hat{h}(\mathbf{r }) \phi_{nlm_lm_s}(r,\sigma) = \epsilon_{n}(Z) \phi_{nlm_lm_{s}}(\mathbf{r},\sigma)
        \end{equation*}
        where the wavefunction factorizes into
        \begin{equation*}
          \phi_{nlm_lm_{s}}(\mathbf{r},\sigma) = R_{nl}(r) Y_{l}^{m_l}(\theta, \phi)\chi_{\sigma}
        \end{equation*}
        The spatial part consist of radial
        \begin{multline*}
          R_{nl }^{(Z)}(r) = \sqrt{ \left( \frac{2Z}{na_0} \right)^{3} \frac{(n - l  - 1)!}{2n[(n + l )!]} } e^{-Zr/(na_0)} \\ \times\left( \frac{2Zr}{na_0} \right)^{l } L_{n}^{2l  + 1 - n - l  - 1} \left( \frac{2Zr}{na_0} \right)
        \end{multline*}
        and angular wavefunction
        \begin{equation*}
          Y_{l }^{m}(\theta, \phi) = (-1)^{m} \sqrt{\frac{(2l  + 1)(l  - m)!}{4\pi(l  + m)!}} P_{l }^{m}(\cos \theta) e^{im\phi}
        \end{equation*}
        The spin part of the wavefunction belongs to a two–dimensional Hilbert space describing a spin-$1/2$ particle, with $\sigma=(+1/2,-1/2)$.
        These stationary eigenfunctions are what are called atomic orbitals, defined by quadruple of quantum numbers
        \begin{equation*}
          n = 1,\ldots \quad l = 0, \ldots, n - 1, \quad m_{l } = -l , \ldots, l , \quad m_{s} = \pm \frac{1}{2}
        \end{equation*}
        An electron bound to the nucleus can only exist in one of these allowed eigenstates rather than in an arbitrary spatial distribution.

  \item \textbf{Orbitals group into shells.} Solving the eigenvalue problem gives the eigenenergies
        \begin{equation*}
          \epsilon_n = - 13.6\frac{Z^2}{n^2}  \; \text{eV}
        \end{equation*}
        which independent of $(l, \, m_l,  m_s)$.
        The number of spatial orbitals for a fixed $\ell$ is $2l+1$, so the degeneracy in $n$ is
        \begin{equation*}
          g_l =\sum_{\ell=0}^{n-1} (2\ell + 1)
          = 2 \sum_{\ell=0}^{n-1} \ell + \sum_{\ell=0}^{n-1} 1
          = 2 \frac{n(n-1)}{2} + n = n^2.
        \end{equation*}
        Each spatial orbital can be combined with two spin states
        \begin{equation*}
          g_n = 2g_s=2n^2
        \end{equation*}

        Hamiltonian possesses degenerate eigenstates labeled by the same principal quantum number $n$ and that these degenerate orbitals form a shell.
        The experimental data suggest that the true number of electrons in each experimental shell are $(2, 8, 8, 18, 18, 32, \ldots)$ instead of the predicted $(2,8,18,32,\ldots)$.
        This reflects subshell energy splitting caused by electron–electron interaction which is ignored in this consideration.

  \item \textbf{Chemical properties repeat periodically with atomic number.}
        Each atom with $Z$ electrons fills the available spin–orbitals in order of increasing energy (subject to the Pauli exclusion principle).
        Because the orbitals $2n^2 $, adding electrons sequentially produces a repeating pattern of:   filled shell, such as noble gasses, which produce stable, low-energy configurations; and partial filled shell, such as alkali metal, which produce more chemically reactive configurations.
\end{enumerate}

\subsection{Variational Principle}
For any normalized many-body state $|\Psi\rangle$, not necessarily an eigenstate, which belongs to the Hilbert space on which acts the Hamiltonian $\hat{H}$, one finds
\begin{equation*}
  \langle \Psi | \hat{H} | \Psi \rangle \geq E_{gs}
\end{equation*}
where $E_{gs}$ is the ground-state energy of the system and the equality holds only if $|\Psi\rangle = |\Psi_{gs}\rangle$ with $|\Psi_{gs}\rangle$ ground-state of the system, such that
\begin{equation*}
  \hat{H} |\Psi_{gs}\rangle = E_{gs} |\Psi_{gs}\rangle
\end{equation*}

\subsubsection{Derivation.}
The many-body Hamiltonian $\hat{H}$ satisfies the exact eigenvalue problem
\begin{equation*}
  \hat{H}|\Psi_\alpha\rangle = E_\alpha |\Psi_\alpha\rangle
\end{equation*}
where $E_\alpha$ are the ordered eigenvalues, such that $E_0 < E_1 < \ldots$; and $|\Psi_\alpha\rangle$ the corresponding orthonormalized eigenstates, such that $\langle \Psi_\alpha|\Psi_\beta \rangle = \delta_{\alpha,\beta}$.
The generic many-body state $|\Psi\rangle$ can be written as
\begin{equation*}
  |\Psi\rangle = \sum_{\alpha} c_\alpha |\Psi_\alpha\rangle
\end{equation*}
where $c_\alpha$ are the complex coefficients of the expansion such that the normalization condition is satisfied.
Then one finds
\begin{align*}
  \langle \Psi | \hat{H} | \Psi \rangle
   & =  \sum_{\alpha,\beta} c_\alpha^* c_\beta E_\beta \langle \Psi_\alpha | \Psi_\beta \rangle = \sum_{\alpha} |c_\alpha|^2 E_\alpha \\
   & \geq \sum_{\alpha} |c_\alpha|^2 E_0                                                                                              \\
  \langle \Psi | \hat{H} | \Psi \rangle
   & \geq E_0
\end{align*}

\subsection{Hartree for Bosons}
Hartree method assumes the total wavefunction is a simple product of single-particle orbitals
\begin{equation*}
  \Psi(r_1, \ldots, r_N) = \prod_{i=1}^{N} \phi(r_i)
\end{equation*}
where the single-particle wavefunction $\phi(x)$ is unknown.
In the variational spirit of the Hartree approach the unknown wavefunction $\phi(x)$ is determined by minimizing the expectation value of the total Hamiltonian, given by
\begin{equation*}
  \langle \Psi | \hat{H} | \Psi \rangle = \int dx_1 dx_2 \ldots dx_N \Psi^*(x_1, x_2, \ldots, x_N) \hat{H} \Psi(x_1, x_2, \ldots, x_N)
\end{equation*}
with respect to $\phi(x)$.
In fact, by using $\hat{H }=\hat{H}_0+\hat{H }_I$ one finds the single-orbital Hartree functional for bosons
\begin{multline*}
  \langle \Psi | \hat{H} | \Psi \rangle = N \int dx \; \phi^*(x) \hat{h}(x) \phi(x) \\
  + \frac{1}{2} N(N - 1) \int dx \, dx' |\phi(x)|^2 V(x, x') |\phi(x')|^2
\end{multline*}

This functional is to be minimized with the normalization constraint to obtain the Hartree equation
\begin{equation*}
    \left[   \hat{h }(x  )+   U_\text{mf }\right] \phi(x)    = \epsilon\phi(x)  
\end{equation*}
where $U_\text{mf}$ is the mean-field potential 
\begin{equation*}
   U_\text{mf}=
   (N-1)\int dx' \;V(x,x') |\phi(x')|^2 
\end{equation*}

The algorithm of this method are as follows.
\begin{enumerate}
  \item \textbf{Initial guess.} Following the variational principle, choose initial guess for normalized orbital $\phi ^{(0)}(x)$.
  \item \textbf{Construct the mean-field.} Using previous orbital, construct the following potential
\begin{equation*}
   U_\text{mf} ^{(n)}=
   (N-1)\int dx' \;V(x,x') |\phi(x') ^{(n)}|^2 
\end{equation*}  
  \item \textbf{Solve Hartree equation.} Solve the Hartree equation to obtain next iteration of orbital $\phi ^{(1)}(x)$
  \begin{equation*}
    \left[   \hat{h }(x  )+   U_\text{mf } ^{(n)}\right] \phi(x)^{(n+1)}    = \epsilon ^{(n+1)}\phi(x)  ^{(n+1)}
\end{equation*}
  \item \textbf{Repeat.} Use the new orbital to recompute the density and the potential, then repeat until the change between successive orbitals becomes negligible $\phi ^{(n+1)}(x)\approx \phi((n))(x)$.
\end{enumerate}

\subsubsection{Derivation: single-orbital Hartree functional for bosons.}
The expectation value of non-interacting Hamiltonian $\hat{H}_0$ is
\begin{equation*}
  \langle \Psi | \hat{H}_0 | \Psi \rangle =\sum_{i=1}^{N}  \int dx_1 \ldots dx_N \; \Psi^*(x_1, \ldots, x_N) \hat{h}(x_i) \Psi(x_1, \ldots, x_N)
\end{equation*}
For fixed $i$, we write
\begin{equation*}
  \Psi^*\hat{h}(x_i) \prod_{j=1}^{N} \phi(x_j) = \Psi^*\left[ \phi(x_1) \ldots \hat{h}(x_i) \ldots \phi(x_N) \right]
\end{equation*}
since $\hat{h}(x_i)$ acts only on the $i$-th factor $\phi(x_i)$ and leaves all others unchanged.
For a fixed $i$, the integral over all coordinates also factorizes
\begin{multline*}
  \int dx_1 \ldots dx_N \prod_{j=1}^{N} \phi^*(x_j) \phi(x_1) \ldots \hat{h }(x_i) \phi(x_i)\ldots\phi(x_N) \\
  = \int dx_i \phi^*(x_i) \hat{h}(x_i) \phi(x_i) \prod_{j \neq i} \int dx_j |\phi(x_j)|^2\\
  = \int dx_i \phi^*(x_i) \hat{h}(x_i) \phi(x_i)
\end{multline*}
and each $i$ term contribute exactly that.
Since there are $N$  terms
\begin{equation*}
  \langle \Psi | \hat{H}_0 | \Psi \rangle
  = N \int dx_i \phi^*(x) \hat{h}(x) \phi(x)
\end{equation*}

The expectation value of interacting Hamiltonian $\hat{H}_I$ is
\begin{equation*}
  \langle \Psi | \hat{H}_I | \Psi \rangle = \frac{1 }{2}\sum_{i \neq j}  \int dx_1 \cdots dx_N  \Psi ^*(x_1, \ldots, x_N) V(x_i, x_j)  \Psi(x_1, \ldots, x_N)
\end{equation*}
For a pair \( (i, j) \), we write
\begin{equation*}
  \prod_{k=1}^{N} \phi^{*}(x_k) V(x_i, x_j) \prod_{l=1}^{N} \phi(x_l)
  =
  \prod_{k=1}^{N} |\phi(x_k)|^2 V(x_i, x_j)
\end{equation*}
For this term, the integral factorizes into
\begin{multline*}
  \int dx_1 \cdots dx_N |\prod_{k=1}^{N} \phi(x_k)|^2 V(x_i, x_j) \\
  = \int dx_i dx_j |\phi(x_i)|^2 V(x_i, x_j) |\phi(x_j)|^2 \prod_{k \neq i,j} \int dx_k |\phi(x_k)|^2\\
  = \int dx dx' |\phi(x)|^2 V(x,x') |\phi(x')|^2
\end{multline*}
and each $i$ term contribute exactly that.
To count how many terms we have, consider $N \times N$ matrix.
The summation range of $i \neq j$ exclude the diagonal elements, hence all that remains are the off-diagonal elements, giving us $N(N-1)$ elements.
Hence
\begin{equation*}
  \langle \Psi | \hat{H}_I | \Psi \rangle
  =\frac{1}{2}N(N-1 ) \int dx dx' |\phi(x)|^2 V(x,x') |\phi(x')|^2
\end{equation*}

\subsubsection{Derivation: minimizing single-orbital Hartree functional.}
We normalized the single-orbital Hartree functional with respect to normalization constraint
\begin{equation*}
  \int dx \, |\phi(x)|^2 = 1
\end{equation*}
For the non-interacting Hamiltonian, we construct functional
\begin{equation*}
  F = N \int dx\; \phi ^*(x)\hat{h }(x)\phi(x) + \lambda \left( \int dx \left| \phi (x)\right|^2 - 1 \right)
\end{equation*}
Minimizing with respect to $\phi$ yields
\begin{equation*}
  \delta{F} = N \int dx \; \delta \phi^{*} (x)\hat{h}(x) \phi(x) + \lambda \int  d x \; \delta\phi^{*} \phi(x)=0
\end{equation*}
For arbitrary variational $\delta\phi$, in order to make sure that the integral is zero is that the other integrand factor is zero, thus 
\begin{equation*}
  N \hat{h }(x  )\phi(x)+\lambda\phi(x)=0
\end{equation*}

For the case of interacting Hamiltonian, the functional reads
\begin{equation*}
  F = \frac{1}{2} N (N - 1) \int d{x} \; dx' \; |\phi(x)|^2 V(x,x') |\phi(x')|^2 + \lambda \left( \int dx \left| \phi (x)\right|^2 - 1 \right)
\end{equation*}
where we used the same constraint.
Again, we vary $\delta\phi$ (with the constraint omitted)
\begin{multline*}
  \delta F = \frac{1}{2} N(N-1) \bigg[ \int dx\;dx' \; \delta \phi ^*(x)  \phi(x)V(x,x') |\phi(x')|^2 \\+ \int dx\;dx' \; |\phi(x)|^2  V(x,x')  \delta\phi ^*(x ) \phi(x)\bigg]\\
  = N (N-1)\int dx\;dx' \; \delta \phi ^*(x)  \phi(x)V(x,x') |\phi(x')|^2
\end{multline*}
Repeating the same argument (now with the constraint included)
\begin{equation*}
  N (N-1)\int dx' \;   \phi(x)V(x,x') |\phi(x')|^2  +\lambda\phi(x)=0
\end{equation*}

Recall that both term are to be constrained with the same function, so the result is simply 
\begin{align*}
    \left(   N \hat{h }(x  )+   N (N-1)\int dx' \;V(x,x') |\phi(x')|^2 \right) \phi(x)
    +\lambda\phi(x) & = 0\\
    \left(   \hat{h }(x  )+   (N-1)\int dx' \;V(x,x') |\phi(x')|^2 \right) \phi(x)    & = \epsilon\phi(x)
\end{align*}
where the second term is the mean-field potential.


\end{document}